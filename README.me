An etch-a-sketch esc page that defaults to a 64 by 64 grid.
On load the grid squares are white, on each mouseover becomes a random colour.
Then after 10 mouseovers each grid square becomes black.

Button at the top of the page asks the user for the number of squares on each axis, the grid rows and columns are changed to this value.